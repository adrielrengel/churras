<p id="notice"><%= notice %></p>

<h1> Fornecedores</h1>

<table  class="table table-bordered">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Cnpj</th>
      <th>Razao social</th>
      <th>Endereco</th>
      <th>Situacao</th>
      <th>Usuario</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fornecedores.each do |fornecedor| %>
      <tr>
        <td><%= fornecedor.nome %></td>
        <td><%= fornecedor.cnpj %></td>
        <td><%= fornecedor.razao_social %></td>
        <td><%= fornecedor.endereco.id rescue 'Endereço nao encontrado' %></td>
        <td><%= fornecedor.situacao %></td>
        <td><%= fornecedor.usuario.nome rescue 'Usuario nao encontrado' %></td>
        <td><%= link_to 'Show', fornecedor %></td>
        <td><%= link_to 'Edit', edit_fornecedor_path(fornecedor) %></td>
        <td><%= link_to 'Destroy', fornecedor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to raw('<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Adicionar Fornecedor'), new_fornecedor_path%>
