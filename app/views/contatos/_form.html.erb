<%= form_for(@contato) do |f| %>
  <% if @contato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contato.errors.count, "error") %> prohibited this contato from being saved:</h2>

      <ul>
      <% @contato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_contato_id %><br>
    <%= f.text_field :tipo_contato_id %>
  </div>
  <div class="field">
    <%= f.label :contato %><br>
    <%= f.text_field :contato %>
  </div>
  <div class="field">
    <%= f.label :contatavel_id %><br>
    <%= f.text_field :contatavel_id %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.text_field :usuario_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
