<%= form_for(@forma_pagamento) do |f| %>
  <% if @forma_pagamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forma_pagamento.errors.count, "error") %> prohibited this forma_pagamento from being saved:</h2>

      <ul>
      <% @forma_pagamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :situacao %><br>
    <%= f.text_field :situacao %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.text_field :usuario_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
