<%= form_for(@salario) do |f| %>
  <% if @salario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salario.errors.count, "error") %> prohibited this salario from being saved:</h2>

      <ul>
      <% @salario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :funcionario_id %><br>
    <%= f.text_field :funcionario_id %>
  </div>
  <div class="field">
    <%= f.label :valor %><br>
    <%= f.text_field :valor %>
  </div>
  <div class="field">
    <%= f.label :saida_caixa_id %><br>
    <%= f.text_field :saida_caixa_id %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.text_field :usuario_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
