<%= form_for(@tipo_contato) do |f| %>
  <% if @tipo_contato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_contato.errors.count, "error") %> prohibited this tipo_contato from being saved:</h2>

      <ul>
      <% @tipo_contato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.text_field :usuario_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
